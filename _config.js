const CONFIG = {
  SETUP_PATH: process.cwd(), // Path of the current folder where the script is being executed
  SCRIPTS_PATH: "/home/thiago/projetos/_scripts", // Path where the scripts (.js) are being stored
  SHELL_SCRIPTS_PATH: "/home/thiago/projetos/_scripts/.sh", // Path where the bash (.sh) scripts are being stored
};

module.exports = CONFIG;
